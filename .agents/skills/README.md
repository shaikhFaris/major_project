# Skills

Skills are reusable, self-contained instructions for accomplishing a task in this project.
To find and install community skills, run:

```
npx skills find <query>
npx skills add <owner/repo> --skill <name> --yes
```

To load an installed skill, use:

```
npx skills run <skill-name>
```
